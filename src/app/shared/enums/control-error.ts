export enum ControlError {
    Required = 'required',
    Minlength = 'minlength',
    Maxlength = 'maxlength',
    Pattern = 'pattern',
    Email = 'email',
    Min = 'min',
    Max = 'max',
    RequiredTrue = 'requiredTrue',
    Custom = 'custom',
}
